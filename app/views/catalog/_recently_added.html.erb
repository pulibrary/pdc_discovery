<div class="text-center">
  <h2 class="h3">Recently published</h2>
  <ul class="text-left" id="recently-added">
    <% RecentlyAdded.feed("http://localhost:3000").each do |key, values| %>
      <%= render_recent_entry(key, values) %>
    <% end %>
  </ul>

</div>
